name: Build plugin zip and attach to release

permissions:
  contents: write

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'Tag to build (vX.Y.Z)'
        required: true
        type: string

jobs:
  build-zip:
    runs-on: ubuntu-latest
    # Auto-build on non-prerelease publish; manual workflow_dispatch always
    # runs (so a prerelease that needs the zip attached can be handled by
    # dispatching with the same tag). Mirrors wporg-deploy.yml's gate.
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.release.prerelease == false }}
    env:
      TAG_NAME: ${{ github.event_name == 'release' && github.event.release.tag_name || inputs.tag_name }}

    steps:
      # All actions pinned to commit SHAs (not moving tags) to guard
      # against an upstream tag being repointed at a malicious commit.
      # Trailing comment is the human-readable version that SHA
      # corresponded to at pin time. .github/dependabot.yml opens grouped
      # weekly PRs to bump them.
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          ref: ${{ env.TAG_NAME }}

      - name: Setup PHP
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240  # v2
        with:
          php-version: '8.3'
          tools: composer:v2

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0
        with:
          node-version: '22.x'
          cache: npm

      - name: Validate tag format (vX.X.X) and versions match
        # TAG comes via env: (not ${{ }} interpolation inside run:) so
        # the shell treats it as a plain variable and a hostile tag
        # value cannot break out into the script. See
        # https://github.blog/security/vulnerability-research/how-to-catch-github-actions-workflow-injections-before-attackers-do/
        env:
          TAG: ${{ env.TAG_NAME }}
        run: |
          set -euo pipefail

          echo "Release tag: ${TAG}"

          if ! [[ "${TAG}" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Tag must look like vX.X.X, for example v0.1.0"
            exit 1
          fi

          TAG_VER="${TAG#v}"
          PKG_VER="$(node -p "require('./package.json').version" | tr -d '\r')"
          PLUGIN_VER="$(
            grep -m1 -E '^[[:space:]]*\*?[[:space:]]*Version:[[:space:]]*' cashu-for-woocommerce.php \
              | sed -E 's/^[[:space:]]*\*?[[:space:]]*Version:[[:space:]]*//' \
              | tr -d '\r'
          )"

          echo "Tag version:           ${TAG_VER}"
          echo "package.json version:  ${PKG_VER}"
          echo "Plugin header version: ${PLUGIN_VER}"

          [ "${TAG_VER}" = "${PKG_VER}" ] || { echo "Mismatch, tag vs package.json"; exit 1; }
          [ "${TAG_VER}" = "${PLUGIN_VER}" ] || { echo "Mismatch, tag vs plugin header"; exit 1; }

      - name: Ensure zip is available
        run: |
          command -v zip >/dev/null 2>&1 || (sudo apt-get update && sudo apt-get install -y zip)

      - name: Build via build.sh
        run: bash ./build.sh

      - name: Upload zip to GitHub Release
        uses: softprops/action-gh-release@efb35369e0ad2afab669f228072c1b0d510eae64  # v3.0.3
        with:
          tag_name: ${{ env.TAG_NAME }}
          overwrite: true
          files: cashu-for-woocommerce.zip
          fail_on_unmatched_files: true
